
package book.kotlin_in_action.part1.chapter_4


/**
 * 코틀린의 기본 가시성은 public
 * package-private은 없다.
 * 코틀린에는 Interval이라는 변경자가 존재.
 * 같은 모듈에서만 볼 수 있는 것이다.
 * 코틀린에서 protecte는 하위 클래스 안에서만 볼 수 있다.
 *
 * 코틀린에서는 외부 클래스가 내부 클래스나 중처뵌 클래스의 private 멤버에 접근할 수 없다.
 *
 */

fun main() {

}